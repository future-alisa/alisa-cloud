# alisa-cloud

基于 Spring Cloud Alibaba 简单封装的基础服务

- 向导
  git clone https://github.com/future-alisa/alisa-cloud.git
  用 ide 打开 alisa-cloud 项目，之后将 alisa-gateway 下 example.env 内容复制到 .env 并修改里面的内容，最后 mvn clean install，然后运行即可。
